---
const props = Astro.props;
---

<style>
  a {
    text-decoration: none;
    cursor: pointer;
  }

  h2::after {
    content: "⛓";
    color: var(--disabled-foreground);
    opacity: 0;
  }

  h2:hover::after {
    opacity: 1;
  }
</style>

<a href={`#${props.id}`}>
  <h2 {...props}>
    <slot />
  </h2>
</a>
