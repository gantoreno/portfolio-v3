---
const props = Astro.props;
---

<style>
  a {
    text-decoration: none;
    cursor: pointer;
  }

  a h2::after {
    content: "⛓";
    color: var(--disabled-foreground);
    opacity: 0;
  }

  a:hover h2::after {
    opacity: 1;
  }
</style>

<a href={`#${props.id}`}>
  <h2 {...props}>
    <slot />
  </h2>
</a>
