---
type Props = {
  name: string;
};

const props = Astro.props;

function slugify(text: string) {
  return text.replace(/\s/g, "-").toLowerCase();
}
---

<style>
  span {
    display: inline-block;
    color: var(--text-foreground);
  }

  span::before {
    content: "#";
  }
</style>

<span>{slugify(props.name)}</span>
